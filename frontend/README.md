# Agent Darwin Frontend

Next.js 15 research dashboard for the Agent Darwin FastAPI backend.

## Run

```bash
npm install
npm run dev
```

The frontend uses `NEXT_PUBLIC_API_BASE_URL`, defaulting to:

```text
http://127.0.0.1:8000/api/v1
```
